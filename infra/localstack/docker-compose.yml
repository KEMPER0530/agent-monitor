services:
  localstack:
    image: localstack/localstack:3
    ports:
      - "4566:4566"
    environment:
      - SERVICES=s3,dynamodb,lambda,apigateway,cloudfront,iam,sts,cloudformation
      - DEBUG=0
    volumes:
      - "/var/run/docker.sock:/var/run/docker.sock"

